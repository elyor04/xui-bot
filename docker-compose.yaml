services:
  xui-bot:
    build: .
    container_name: xui_bot
    restart: unless-stopped
    environment:
      - TZ=Asia/Tashkent
    volumes:
      - ./.env:/app/.env:ro
      - ./bot.sqlite3:/app/bot.sqlite3
